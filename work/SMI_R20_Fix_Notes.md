# SMI R20 修复对照说明（Fix Notes）

- 基线：R19 送审 HEAD `dbc0fae`
- 本轮范围：R19 裁定 R17-P2-01 仍未闭环——正则 `$` 放过尾部换行（"3.2\n"）、`\d` 放过 Unicode 数字（"3２.2"/"3٢.2"）
- 日期：2026-08-22

## 修复：解析器升级为 ASCII 全串匹配（R19 §9 推荐语义）

**裁定：采纳。** 两个 Python 正则语义坑：`$` 匹配尾部换行前位置；`\d` 默认是 Unicode 十进制数字类。

1. `_STRICT_VERSION_RE = r"(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)"` + **`fullmatch()`**（不依赖行尾锚）+ **`[0-9]`**（非 Unicode 数字类）+ 段长 <=9（超长数字拒绝，int 永不抛异常）；
2. `strict_v42` 与 `tracksVersionSchedule` 两处消费不变（仍是唯一解析器）；
3. cutoff / 时间表 / 历史白名单 / v4 状态机零改动。

## 测试（R19 §10 全表落地）

- `test_strict_version_parser_matrix`（解析器单测，R19 §9 语义表全项）：
  - PASS：`0.0`/`3.2`/`4.0`/`32.2`/`1.0`/`2.0` → 唯一元组；
  - FAIL（None）：`03.02`/`3.02`/`3`/`3.`/`3.2.1`/`3.2.`/`3.2x`/`3.2 `/` 3.2`/**`3.2\n`**/**`3２.2`（全角）**/**`3٢.2`（阿拉伯-印度）**/`legacy`/`""`/`1234567890.0`（超长）/None/非字符串；
- `test_tracks_v4_version_schedule_unicode_and_newline_rejected`（集成）：cutoff 后 `3.2\n`/`3２.2`/`3٢.2`/超长版本 → FAIL。

## 验证证据（2026-08-22）

- 解析器矩阵 6 PASS + 17 FAIL 形态独立验证通过；
- pytest 全量 **299 passed + 1 skipped**（R19 后 297 净增 2）；
- acceptance --all：PASS=2（07-17、08-20）不变。

## 已知边界（不变）

沿用 R15~R19 裁定。
