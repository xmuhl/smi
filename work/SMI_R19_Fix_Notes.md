# SMI R19 修复对照说明（Fix Notes）

- 基线：R18 送审 HEAD `f2b1813`
- 本轮范围：R18 裁定 R17-P2-01 仍未闭环——"严格 x.y"解析不严格（3.2.1/3.2./4/尾空白/03.02 可绕）
- 日期：2026-08-22

## 修复：唯一严格版本解析器（R18 §9.1 方案）

**裁定：采纳。**

1. **`_parse_strict_version()`**（accept.py 模块级唯一解析器）：正则 `^(0|[1-9]\d*)\.(0|[1-9]\d*)$`——仅接受规范 x.y：
   - 合法：`3.2`→(3,2)、`4.0`→(4,0)、`1.0`/`2.0`/`3.0`/`3.1`
   - 非法（None）：`legacy`、`3.x`、`3.2.1`、`3.2.`、`3.2 `、` 3.2`、`4`、`3`、`""`、`03.02`、`3.02`（避免多字符串映射同一元组）
2. **strict_v42 与时间表检查统一走该解析器**（消除两处各自为政的宽松 split/int 解析）：cutoff（from 2026-08-21）后任何不可规范解析的版本 → FAIL（"非规范 x.y 数值版本…版本降级旁路"）；可解析但 <3.2 → FAIL。
3. **测试**（R18 §10 清单全落地）：
   - `test_tracks_v4_version_schedule_malformed_versions_rejected`：cutoff 后 `3.2.1`/`3.2.`/`4`/`3.2 `/` 3.2`/`03.02`/`""` 7 形态全部 FAIL；
   - `test_tracks_v4_version_schedule_future_40_passes`：cutoff 后 `4.0`（规范且 >=3.2）+ 完整 strict 字段 → PASS（未来版本演进不受阻）；
   - 存量保持：future+3.0/legacy/3.x FAIL、08-20+3.0 PASS、future+3.2 PASS（既有测试）。
4. **历史白名单不受影响**：`<=2026-08-20` 仍是精确字符串白名单（legacy/1.0/2.0/3.0/3.1/3.2），本轮零历史文件改动。

## 验证证据（2026-08-22）

- pytest 全量 **297 passed + 1 skipped**（R18 后 295 净增 2）；
- acceptance --all：PASS=2（07-17、08-20）不变。

## 已知边界（不变）

沿用 R15~R18 裁定。
