# SMI VPS 日志切割（VPS_MIGRATION_PROGRESS_20260901 任务 6）
# 安装: cp 到 /etc/logrotate.d/smi（部署脚本自动完成）
/opt/smi/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
}
